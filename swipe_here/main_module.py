import swipe , zoom , pointer
from threading import Thread

# s = Thread(target=swipe.main())
# z = Thread(target=zoom.main())
# p = Thread(target=pointer)

# s.start()
# z.start()
# p.start()